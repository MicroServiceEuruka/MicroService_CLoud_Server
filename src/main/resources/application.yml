server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/MicroServiceEuruka/MicroService_CLoud_Server
          default-label: main
        bootstrap: false